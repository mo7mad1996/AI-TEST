export enum AppType {
  REGULAR = 'regular',
  AGENT = 'agent',
}
export enum AgentRole {
  ADMIN = 'admin',
  AGENT = 'agent',
}

export enum RegularRoles {
  INDIVIDUAL = 'individual',
  BUSINESS = 'business',
}

export enum BusinessPosition {
  DIRECTOR = 'director',
  EMPLOYEE = 'employee',
  OWNER = 'owner',
}
